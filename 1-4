#include <iostream>

using namespace std;

int main() {
	int num;
	cout << "Ingrese un numero de 3 cifras: ";
	cin >> num;
	string verificacion = (num > 999 || num < 100) ? "Numero Incorrecto" : "Numero Correcto";

	cout << verificacion;

	return 0;
}
